apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # node.yaml removed — Tdarr node runs as Proxmox LXC on pve02 for direct /dev/dri GPU access
  - server.yaml
  - service.yaml
  - tls.yaml
