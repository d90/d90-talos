apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.prowlarr
  namespace: default
spec:
  dnsNames:
    - prowlarr.d90.name
  secretName: tls.prowlarr
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
