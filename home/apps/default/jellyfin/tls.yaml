apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.jellyfin
  namespace: default
spec:
  dnsNames:
    - jellyfin.d90.name
  secretName: tls.jellyfin
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
