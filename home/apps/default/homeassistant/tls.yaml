apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.hass
  namespace: default
spec:
  dnsNames:
    - hass.d90.name
  secretName: tls.hass
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
