apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: &name tls.radarr
  namespace: default
spec:
  dnsNames:
    - radarr.d90.name
  secretName: *name
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
