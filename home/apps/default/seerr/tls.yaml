apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.seerr
  namespace: default
spec:
  dnsNames:
    - seerr.d90.name
  secretName: tls.seerr
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
