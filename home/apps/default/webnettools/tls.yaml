apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.webnettools
  namespace: default
spec:
  dnsNames:
    - wnt.d90.name
  secretName: tls.webnettools
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
