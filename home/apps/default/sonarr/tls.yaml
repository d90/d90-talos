apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.sonarr
  namespace: default
spec:
  dnsNames:
    - sonarr.d90.name
  secretName: tls.sonarr
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
