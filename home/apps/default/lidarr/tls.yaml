apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.lidarr
  namespace: default
spec:
  dnsNames:
    - lidarr.d90.name
  secretName: tls.lidarr
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
