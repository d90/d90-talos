apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.homepage
  namespace: default
spec:
  dnsNames:
    - homepage.d90.name
  secretName: tls.homepage
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
