apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.watchstate
  namespace: default
spec:
  dnsNames:
    - watchstate.d90.name
  secretName: tls.watchstate
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer