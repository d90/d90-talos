apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.rtunnel
  namespace: default
spec:
  dnsNames:
    - rtunnel.d90.name
  secretName: tls.rtunnel
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
