apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.una
  namespace: default
spec:
  dnsNames:
    - una.d90.name
  secretName: tls.una
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
