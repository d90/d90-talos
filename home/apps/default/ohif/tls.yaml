apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.ohif
  namespace: default
spec:
  dnsNames:
    - ohif.d90.name
  secretName: tls.ohif
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer
