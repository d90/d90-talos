apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls.scrypted
  namespace: default
spec:
  dnsNames:
    - scrypted.d90.name
  secretName: tls.scrypted
  privateKey:
    algorithm: ECDSA
    size: 256
  revisionHistoryLimit: 1
  issuerRef:
    name: le-prod-issuer
    kind: ClusterIssuer